Heartbeat check for {{workspace}} workspace at {{datetime}}.

1. Run `git status` to check for uncommitted changes
2. Check for any failing tests or lint errors
3. List open PRs that need attention
4. Report any issues found

Be concise — this is an automated heartbeat check. Only report actionable items.
