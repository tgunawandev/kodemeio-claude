Heartbeat check for {{workspace}} workspace at {{datetime}}.

1. Run `git status` across key repos to check for uncommitted changes
2. Check if any CI/CD pipelines have failed recently
3. Look for open PRs that need review
4. Report any issues found

Be concise — this is an automated health check. Only report actionable items.
